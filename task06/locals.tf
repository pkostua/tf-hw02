locals {
  vm_web_name = "${var.project_name}-${var.area_develop_name}-${var.resource_type_platform_name}-web"
  vm_db_name = "${var.project_name}-${var.area_develop_name}-${var.resource_type_platform_name}-db"
}
